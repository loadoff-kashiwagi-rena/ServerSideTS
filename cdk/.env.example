# CloudFront + OAC 検証スタック用の設定
# このファイルをコピーして cdk/.env を作成し、値を埋める（.env は gitignore 済み）。
#
#   cp .env.example .env
#
# 検証用 S3 バケット名（グローバルで一意。既存と衝突しない新規名にする）。
# このバケットは CloudFrontOacStack が新規作成し、完全プライベート + OAC でのみアクセスする。
OAC_VERIFY_BUCKET=handson-cloudfront-oac-verify-xxxxx
